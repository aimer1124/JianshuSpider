div#user
    if result.length == 0
        tr
            td 没有搜索到任何内容
    else
        table(data-toggle="table", data-sort-name="follower", data-sort-order="desc")
            thead
                tr
                    th(data-field="author", data-sortable="false") 作者
                    th(data-field="favorite", data-sortable="true") 收获喜欢
                    th(data-field="follower", data-sortable="true") 粉丝
            tbody
                each user in result
                    tr
                        td
                            a(href='http://www.jianshu.com' + '#{user.id}') #{user.author}
                        td
                            span #{user.favorite}
                        td
                            span #{user.follower}