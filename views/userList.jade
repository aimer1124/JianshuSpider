div#user
    if result.length == 0
        tr
            td 没有搜索到任何内容
    else
        table
            thead
                tr
                    td 作者
                    td 关注
                    td 粉丝
            tbody
                each user in result
                    tr
                        td
                            a(href='http://www.jianshu.com' + '#{user.id}') #{user.author}
                        td
                            span #{user.following}
                        td
                            span #{user.follower}